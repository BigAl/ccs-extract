version: '3.8'

services:
  ccs-extract:
    build: .
    volumes:
      - ./input:/app/input
      - ./output:/app/output
      - ./transaction_config.json:/app/transaction_config.json
    environment:
      - PYTHONUNBUFFERED=1 